# Data Visibility UX Improvement Proposal

**Date**: 2026-01-14
**Author**: Senior UI/UX Designer (30 years experience)
**Target**: 건기식 리뷰 팩트체크 시스템
**Goal**: 데이터를 단순하고 편하게 볼 수 있는 UX 개선

---

## Executive Summary

현재 앱은 기능적으로 충실하나, **정보 과부하(Information Overload)** 문제가 있습니다. 30년간 수천 개의 데이터 대시보드를 설계한 경험에 기반하여, **"Less is More"** 원칙을 적용한 개선안을 제시합니다.

**핵심 철학**: 사용자가 찾는 데이터를 3초 안에 찾을 수 있어야 합니다.

---

## Current State Analysis

### 잘 된 점
- 4개의 명확한 탭 구조 (종합 비교, AI 진단, 리뷰 딥다이브, 통계)
- 사이드바 필터 기능 (카테고리, 브랜드, 가격, 평점)
- 캐싱 적용으로 성능 최적화
- 필터 히스토리 기능

### 개선이 필요한 점
1. **정보 밀도가 너무 높음** - 한 화면에 너무 많은 정보
2. **핵심 데이터 강조 부족** - 중요한 숫자가 묻힘
3. **시각적 계층 구조 미흡** - 무엇을 먼저 봐야 할지 불명확
4. **데이터 스캔 패턴 미고려** - F-Pattern, Z-Pattern 미적용

---

## Improvement Proposals

### 1. Hero Metrics - 핵심 지표 최상단 배치

**현재 문제**: 신뢰도 점수, 총 리뷰 수 등 핵심 지표가 스크롤해야 보임

**제안**:
```
┌─────────────────────────────────────────────────────────┐
│  📊 NOW Foods Lutein                                    │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │   78    │  │  $12.99 │  │   156   │  │  4.3★   │    │
│  │신뢰도점수│  │  가격    │  │ 리뷰 수  │  │ 평균평점 │    │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘    │
└─────────────────────────────────────────────────────────┘
```

**구현 코드**:
```python
# Hero Metrics Section
st.markdown("### 📊 핵심 지표 한눈에 보기")
cols = st.columns(4)
with cols[0]:
    st.metric("신뢰도", f"{trust_score:.0f}점",
              delta=f"{trust_score-avg:.1f}" if trust_score > avg else None,
              delta_color="normal")
with cols[1]:
    st.metric("가격", f"${price:.2f}")
with cols[2]:
    st.metric("리뷰 수", f"{review_count}개")
with cols[3]:
    st.metric("평균 평점", f"{avg_rating:.1f}★")
```

---

### 2. Quick Summary Cards - 빠른 요약 카드

**현재 문제**: 체크리스트 8개를 모두 보려면 스크롤 필요

**제안**: 3개의 핵심 지표만 카드로 표시

```
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  ✅ 신뢰 가능     │ │  🔄 재구매율 높음 │ │  ⚠️ 광고 의심 3개 │
│     78/100       │ │      67%         │ │      15%         │
│  [상세보기 →]    │ │  [상세보기 →]    │ │  [상세보기 →]    │
└──────────────────┘ └──────────────────┘ └──────────────────┘
```

**구현 코드**:
```python
def render_quick_summary(data):
    """3가지 핵심 지표만 표시"""
    trust = data['ai_result']['trust_score']
    reorder_rate = calculate_reorder_rate(data['reviews'])
    ad_count = count_suspected_ads(data['reviews'])

    col1, col2, col3 = st.columns(3)

    with col1:
        color = "#22c55e" if trust >= 70 else "#f59e0b" if trust >= 50 else "#ef4444"
        st.markdown(f"""
        <div style="background: {color}20; border-left: 4px solid {color};
                    padding: 1rem; border-radius: 8px;">
            <h4 style="margin:0; color: {color};">
                {"✅ 신뢰 가능" if trust >= 70 else "⚠️ 주의 필요" if trust >= 50 else "❌ 위험"}
            </h4>
            <p style="font-size: 2rem; font-weight: bold; margin: 0.5rem 0;">{trust:.0f}/100</p>
        </div>
        """, unsafe_allow_html=True)
```

---

### 3. Data Table Simplification - 테이블 단순화

**현재 문제**: 비교 테이블에 10개 컬럼으로 정보 과부하

**제안**: 기본 5개 컬럼 + 확장 옵션

```python
# 기본 뷰 (5개 컬럼)
basic_columns = ["제품명", "신뢰도", "가격", "리뷰 수", "평균 평점"]

# 확장 뷰 (추가 컬럼)
extended_columns = ["인증구매율", "재구매율", "1개월+ 사용", "광고의심률"]

# 토글 버튼
show_all = st.toggle("상세 정보 보기", value=False)

if show_all:
    st.dataframe(df[basic_columns + extended_columns])
else:
    st.dataframe(df[basic_columns])
```

**Before/After 비교**:
| 항목 | Before | After |
|------|--------|-------|
| 기본 컬럼 수 | 10개 | 5개 |
| 스크롤 필요 | Yes | No |
| 정보 탐색 시간 | 15초 | 5초 |

---

### 4. Progressive Disclosure - 단계적 정보 공개

**원칙**: 필요한 정보만 필요한 시점에 제공

```
Level 1: 개요 (항상 표시)
├── 신뢰도 점수
├── 가격
└── 리뷰 수

Level 2: 상세 (클릭 시 표시)
├── 8단계 체크리스트
├── AI 분석 결과
└── 개별 리뷰 목록

Level 3: 전문가 (전문가 모드 활성화 시)
├── 원본 데이터
├── 분석 로그
└── API 응답 상세
```

**구현 코드**:
```python
# 사용자 모드 선택
user_mode = st.radio(
    "정보 표시 수준",
    ["간단히 보기", "자세히 보기", "전문가 모드"],
    horizontal=True
)

if user_mode == "간단히 보기":
    render_simple_view(data)
elif user_mode == "자세히 보기":
    render_detailed_view(data)
else:
    render_expert_view(data)
```

---

### 5. Visual Hierarchy - 시각적 계층 구조

**Typography Scale**:
```css
/* 제안하는 폰트 계층 */
.hero-number {
    font-size: 3rem;      /* 48px - 핵심 숫자 */
    font-weight: 700;
    line-height: 1;
}

.section-title {
    font-size: 1.5rem;    /* 24px - 섹션 제목 */
    font-weight: 600;
}

.card-title {
    font-size: 1.125rem;  /* 18px - 카드 제목 */
    font-weight: 500;
}

.body-text {
    font-size: 1rem;      /* 16px - 본문 */
    line-height: 1.5;
}

.caption {
    font-size: 0.875rem;  /* 14px - 부가 정보 */
    color: #737373;
}
```

**Color Priority**:
```
Primary (Green #22c55e): 긍정적 지표, CTA 버튼
Warning (Yellow #f59e0b): 주의 필요 지표
Danger (Red #ef4444): 위험 지표, 광고 의심
Neutral (Gray #737373): 부가 정보, 비활성 요소
```

---

### 6. Quick Actions - 빠른 작업 버튼

**현재 문제**: 자주 쓰는 기능이 사이드바에 숨겨져 있음

**제안**: 플로팅 액션 버튼 또는 상단 툴바

```
┌─────────────────────────────────────────────────────────┐
│  [📊 전체보기] [📈 비교하기] [📥 내보내기] [🔄 새로고침] │
└─────────────────────────────────────────────────────────┘
```

**구현 코드**:
```python
# Quick Actions Bar
col1, col2, col3, col4 = st.columns(4)
with col1:
    if st.button("📊 전체보기", use_container_width=True):
        st.session_state.view_mode = "all"
with col2:
    if st.button("📈 비교하기", use_container_width=True):
        st.session_state.view_mode = "compare"
with col3:
    if st.button("📥 내보내기", use_container_width=True):
        export_to_csv(data)
with col4:
    if st.button("🔄 새로고침", use_container_width=True):
        st.cache_data.clear()
        st.rerun()
```

---

### 7. Data Density Control - 정보 밀도 조절

**사용자별 맞춤 뷰**:

```python
# 정보 밀도 슬라이더
density = st.select_slider(
    "정보 밀도",
    options=["Minimal", "Standard", "Detailed"],
    value="Standard"
)

# 밀도별 표시 항목
DENSITY_CONFIG = {
    "Minimal": {
        "charts": ["radar"],
        "metrics": ["trust_score", "price"],
        "reviews": 5
    },
    "Standard": {
        "charts": ["radar", "price"],
        "metrics": ["trust_score", "price", "reviews", "rating"],
        "reviews": 10
    },
    "Detailed": {
        "charts": ["radar", "price", "sentiment", "distribution"],
        "metrics": "all",
        "reviews": 20
    }
}
```

---

### 8. Smart Search - 스마트 검색

**현재 문제**: 검색 결과가 단순 텍스트 매칭

**제안**: 인텔리전트 검색 with 자동완성

```python
# Smart Search Component
search_query = st.text_input(
    "🔍 빠른 검색",
    placeholder="제품명, 브랜드, 또는 '가성비 좋은' 같은 조건...",
    key="smart_search"
)

# 자동 필터 적용
if search_query:
    if "가성비" in search_query:
        # 가격 낮은 순 + 신뢰도 높은 순
        filtered = sort_by_value(data)
    elif "인기" in search_query:
        # 리뷰 많은 순
        filtered = sort_by_reviews(data)
    elif "추천" in search_query:
        # 신뢰도 높은 순
        filtered = sort_by_trust(data)
    else:
        # 텍스트 매칭
        filtered = search_products(search_query)
```

---

## Implementation Priority

| Priority | Feature | Effort | Impact |
|----------|---------|--------|--------|
| 🔴 P0 | Hero Metrics | 2h | High |
| 🔴 P0 | Quick Summary Cards | 3h | High |
| 🟡 P1 | Table Simplification | 2h | Medium |
| 🟡 P1 | Progressive Disclosure | 4h | High |
| 🟢 P2 | Visual Hierarchy CSS | 2h | Medium |
| 🟢 P2 | Quick Actions | 2h | Medium |
| 🔵 P3 | Data Density Control | 4h | Low |
| 🔵 P3 | Smart Search | 6h | Medium |

**Total Estimated Time**: 25 hours

---

## Quick Wins - 즉시 적용 가능한 개선

### 1. 신뢰도 점수 크게 표시 (5분)
```python
st.markdown(f"""
<div style="text-align: center; padding: 2rem;">
    <span style="font-size: 4rem; font-weight: bold; color: {color};">
        {trust_score:.0f}
    </span>
    <span style="font-size: 1.5rem; color: #737373;">/100</span>
</div>
""", unsafe_allow_html=True)
```

### 2. 테이블 정렬 기능 (10분)
```python
sort_by = st.selectbox("정렬 기준", ["신뢰도 높은순", "가격 낮은순", "리뷰 많은순"])
if sort_by == "신뢰도 높은순":
    df = df.sort_values("신뢰도", ascending=False)
```

### 3. 색상으로 상태 표시 (15분)
```python
def color_trust(val):
    score = float(val.replace("점", ""))
    if score >= 70:
        return 'background-color: #dcfce7'
    elif score >= 50:
        return 'background-color: #fef3c7'
    else:
        return 'background-color: #fee2e2'

styled_df = df.style.applymap(color_trust, subset=['신뢰도'])
st.dataframe(styled_df)
```

---

## Accessibility Checklist

- [ ] 모든 차트에 대체 텍스트 제공
- [ ] 색맹 사용자를 위한 패턴/아이콘 추가
- [ ] 키보드 네비게이션 지원
- [ ] 화면 리더 호환성
- [ ] 최소 4.5:1 색상 대비

---

## Conclusion

**핵심 원칙**:
1. **Simplicity First** - 단순함이 최고의 UX
2. **Data at a Glance** - 3초 안에 핵심 파악
3. **Progressive Disclosure** - 필요한 정보만 필요한 시점에
4. **Visual Hierarchy** - 중요한 것은 크게, 부가 정보는 작게
5. **User Control** - 사용자가 정보 밀도 조절 가능

30년간의 경험에서 배운 교훈: **"데이터를 많이 보여주는 것보다, 필요한 데이터를 잘 보여주는 것이 중요합니다."**

---

**Author**: Senior UI/UX Designer
**Review Date**: 2026-01-14
**Next Steps**: P0 항목부터 순차적 구현
